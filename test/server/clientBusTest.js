require('kth-node-configuration').overrideDefaults({path : 'config', isRelativePath : false});

var assert = require('assert');
var request = require('request');
var should = require('chai').should();
//var paths = require('server/init/routing/paths');

describe('Unit tests', function() {

  describe('Config', function(){
    //it('should be the correct port', function(done){
    //  var serverConf = require('server/init/configuration');
    //  assert.equal(serverConf.full.port, 3000);
    //  done();
    //});
  });
});